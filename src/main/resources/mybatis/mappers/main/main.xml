<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.bookhub.main.mapper.AladinMapper">

    <!-- 수정해야함
    <insert id="insertBook" parameterType="com.example.bookhub.main.vo.Book">
        INSERT INTO BOOK
            (title, link, pub_date, description, isbn13, price_standard, best_rank)
        VALUES
            (#{bookName}, #{bookPublishedDate}, #{bookPublishedDate}, #{bookDescription}, #{isbn}, #{bookListPrice}, #{priceStandard}, #{bestRank})
    </insert>
    -->



</mapper>